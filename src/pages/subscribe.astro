---
/* P47 WP EXIT: /subscribe/ newsletter page (bundle page 274). Intentionally
   noindex + out of the sitemap per the migration handoff. Its hero image is
   the author's own file rescued from the images host (origin 404). */
import StaticPage from '../layouts/StaticPage.astro';
---
<StaticPage title="Newsletter paused" fullTitle="Newsletter paused - Androidscroll" desc="The AndroidScroll newsletter is paused. No signup form is live; read the guides or follow us for site news." kicker="Newsletter" bodyFile="subscribe" robots="noindex, nofollow">
</StaticPage>
